These standalone probes are for manual FreeRDP connection debugging and are not
part of the SwiftRDP runtime.

They require explicit connection details:

```sh
DirectRDPProbe <host> <user> <password> [port]
HeadlessRDP <host> <user> <password> [port]
```
